# Legacy repair scripts

These files are historical one-off inspection and repair helpers retained for
reference. They are not loaded by the player or deployment workflow. New
verification code belongs in `scripts/` or `tests/` instead.
