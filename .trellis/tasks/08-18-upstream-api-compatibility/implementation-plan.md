# 实施计划

1. 新增独立的上游响应解码模块，并写单元测试。
2. 接入现有有界请求重试层，核对 4xx/5xx 重试边界。
3. 注册 Pages 预缓存和发布资源契约，递增 Service Worker 缓存版本并重算哈希。
4. 更新验证记录，运行完整 `npm run verify`。
5. 只提交本步文件，推送后等待 quality/deploy，使用完整 commit 做线上验收。

后续多音源功能另建任务，必须先验证歌曲 ID、歌词、歌单导入导出和云同步的字符串兼容性。
