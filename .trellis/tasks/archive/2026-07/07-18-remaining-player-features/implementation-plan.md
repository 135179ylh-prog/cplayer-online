# Implementation Plan - 剩余播放与备份功能

## Steps

1. [x] 审计现有播放、歌单、设置页和历史补丁，固定数据契约与失败边界。
2. [x] 扩展音乐资料库 UI，完成最近播放存储、渲染、重播与清空。
3. [x] 完成版本化备份生成、下载、全量校验和 IndexedDB 原子导入。
4. [x] 统一播放模式值，加入 token 化播放尝试与有界自动跳过。
5. [x] 修复设置页损坏标签，提升 PWA 缓存版本并生成 `_check.js`。
6. [x] 运行静态断言、真实浏览器存储/导入/失败路径与桌面/移动布局验证。
7. [x] 更新总任务文档，提交、归档并记录 Trellis 会话。

## Verification

- `node --check _check.js` 与 `node --check sw.js`。
- `git diff --check`、任务上下文校验和功能结构断言。
- 浏览器注入受控歌曲/API/媒体结果，验证最近播放、50 条上限、导入原子性、跳过顺序、全失败停止和自动播放拦截。
- 桌面与 390px 移动视口截图检查。
