# Implementation Plan - 发布可追踪与手机可靠性冲刺

## 当前阶段

进行中：发布产物可追踪、Service Worker 预缓存自动约束、移动生命周期回归和线上 Pages 验收。

## 步骤

1. [x] 读取项目规范、确认基线提交和保护用户未提交改动。
2. [x] 建立 `08-15-release-mobile-reliability` 五份 Trellis 文档。
3. [x] 检查并实现公开构建标识和预缓存资源契约。
4. [x] 根据官方资料处理 Pages workflow 的 Node.js 20 弃用风险。
5. [x] 补齐移动播放、搜索、网络和自动播放失败的回归证据；本阶段确认现有状态机已覆盖缺口，无需重复改写。
6. [x] 校验诊断脱敏边界和本机存储边界；诊断回归确认只保留有界、脱敏的本机信息。
7. [x] 运行定向桌面/手机测试、静态门禁和完整 `npm run verify`；最终本地门禁为 10/10、47/47、260/260、12 跳过、0 vulnerabilities。
8. [x] 运行独立 Trellis 质量检查与 Claude 只读审阅；本地质量检查通过，Claude 两次入口因上游 API 超时未给出通过结论，已记录为外部审阅受限。
9. [ ] 只暂存本阶段文件，独立提交；确认用户原有 5 个改动仍未被暂存或改变。
10. [ ] 推送 `main`，等待 Pages quality/deploy 成功，用直接 CDP 线上验收并更新验证文档。

## 安全约束

- 所有涉及用户数据的浏览器测试使用专用临时数据，结束后清理；不删除用户已有歌单。
- 不把 `.trellis/tasks/07-25-playlist-trash-history/` 下的现有改动加入本阶段提交。
- 不在诊断、构建元数据、日志或提交内容中写入 API 密钥、API 地址、token 或完整音频地址。
